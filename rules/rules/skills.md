## Skills — always prefer skills
- Before doing **any** task or operation, first check whether a relevant skill exists (in the available-skills list). If one does, use it via the Skill tool instead of doing the work manually.
- **Git commit / push:** in **every** project, always go through the `commit` skill (via the Skill tool) — never run raw `git commit` / `git push` directly.
