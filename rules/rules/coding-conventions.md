## Coding conventions — follow them when writing code
- Whenever you write or edit code, follow the project's coding conventions — naming, file/folder placement, formatting, size limits and idioms — matching the surrounding code rather than inventing your own style.
- If a `coding-conventions` skill (or the project's own style guide) is available, apply it **before and while** coding, not as an afterthought.
